@model List<DevTutorialWebApp.Models.Roadmap>
@{
    ViewData["Title"] = "Web Development";
}

<div class="main-container">
    <div class="hero-section">
        <h1 class="hero-title">Web Development</h1>
        <p class="hero-subtitle">Master the art of building modern web applications from frontend to backend.</p>
    </div>

    <div class="container">
        <div class="row">
            <div class="col-12">
                @foreach (var roadmap in Model)
                {
                    <partial name="_RoadmapCard" model="roadmap" />
                }
            </div>
        </div>
    </div>
</div>